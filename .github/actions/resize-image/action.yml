name: "Resize image"
description: "Resize a image collection using Libvips, with Lanczos kernel algorithm"
author: "Yeray David Rodriguez"
inputs:
  images_path:
    description: "Path with the TIFF files to resize"
    required: true
  width:
    description: "Maximum destination image width"
    required: true
  height:
    description: "Maximum destination image height"
    required: true
runs:
  using: "docker"
  image: "Dockerfile"

